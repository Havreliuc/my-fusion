# FNXC:RepoAssets 2026-07-29-01:15:
# Git LFS removed in this decoupled clone. The upstream fork network's LFS budget is
# exhausted, so the demo/docs media objects (demo/**, docs/screenshots/**, screenshots/**)
# are not downloadable, and every LFS-filtered checkout — including the `git worktree add`
# Fusion performs for each task — hard-failed on the smudge filter.
# The media files remain committed as tiny LFS pointer stubs; they are cosmetic doc assets
# only and never used at app runtime.
# Do not re-add `filter=lfs` lines unless the objects are re-hosted somewhere accessible.
